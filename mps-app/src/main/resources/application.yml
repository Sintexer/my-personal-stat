spring:
  data:
    mongodb:
      host: 127.0.0.1
      port: 27017
      database: mps-db
      username: ${MONGO_INITDB_ROOT_USERNAME}
      password: ${MONGO_INITDB_ROOT_PASSWORD}
  webflux:
    base-path: /api

springdoc:
  swagger-ui:
    path: /swagger-ui